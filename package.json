{
  "name": "obsicheck",
  "version": "0.1.0",
  "private": true,
  "description": "All your Obsidian checkboxes in one place.",
  "scripts": {
    "dev": "node esbuild.config.mjs --watch",
    "build": "tsc --noEmit && node esbuild.config.mjs",
    "test": "tsx --test tests/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "esbuild": "^0.28.2",
    "obsidian": "^1.13.1",
    "tsx": "^4.23.15",
    "typescript": "^7.0.2"
  }
}
